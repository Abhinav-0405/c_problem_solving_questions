#include<stdio.h>
int main(){
    int a,b;
    printf("enter a and b");
    scanf("%d%d",&a,&b);
    if(a>b)
    {
        printf("a is grater than b");
    }
    else
    {
        printf("b is greater than a");

    }
    return 0;
}